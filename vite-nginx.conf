server {
    listen 80;

    location /app/mood-check-in {
        alias /usr/share/nginx/html/app/mood-check-in;
        index index.html index.htm;
        try_files $uri $uri/ /app/mood-check-in/index.html;
    }

    location = / {
        return 301 /app/mood-check-in/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}